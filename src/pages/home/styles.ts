import styled from 'styled-components';

export const Button = styled.button`
    background: rgba(64, 202, 240, 0.575);
    color: black;
    font-size: 1em;
    margin: 1em;
    padding: 0.25em 1em;
    border: 2px solid rgba(64, 202, 240, 0.575);
    border-radius: 3px;
`;
